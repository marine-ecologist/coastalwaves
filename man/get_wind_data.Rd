% Generated by roxygen2: do not edit by hand
% Please edit documentation in r/get_wind_data.R
\name{get_wind_data}
\alias{get_wind_data}
\title{Download NOAA Blended Daily Winds for an sf bounding box}
\usage{
get_wind_data(sf_obj, start_date, end_date, zlev = 10)
}
\arguments{
\item{sf_obj}{An \code{sf} object. The bounding box (\code{st_bbox}) is used to
define the spatial extent (latitude and longitude).}

\item{start_date}{Character string, start date in \code{"YYYY-MM-DD"} format.}

\item{end_date}{Character string, end date in \code{"YYYY-MM-DD"} format.}

\item{zlev}{Numeric, vertical level in meters. Default = \code{10.0}.}
}
\value{
A \code{data.frame} with the requested wind data (\code{u_wind}, \code{v_wind},
\code{time}, \code{latitude}, \code{longitude}, and derived \code{wind_speed} and
\code{wind_bearing}).
}
\description{
This function queries the NOAA CoastWatch ERDDAP
\code{noaacwBlendedWindsDaily} dataset for \code{u_wind} and \code{v_wind} components
over the bounding box of an input \code{sf} object and a specified date range.
}
\details{
The function constructs a direct ERDDAP CSV request URL and downloads
the \code{u_wind} and \code{v_wind} variables for the given spatial and temporal
extent. Results are returned as a data frame with additional columns:
\describe{
\item{wind_speed}{Wind speed in m/s calculated as
\eqn{\sqrt{u^2 + v^2}}}
\item{wind_bearing}{Wind direction in degrees clockwise from north,
derived from \code{atan2(u, v)}.}
}
}
\examples{
\dontrun{
library(sf)
pt <- st_point(c(144, -11)) |>
  st_sfc(crs = 4326) |>
  st_sf()

wind_df <- get_wind_data(
  pt,
  start_date = "1988-09-01",
  end_date   = "1988-09-03"
)
head(wind_df)
}

}
